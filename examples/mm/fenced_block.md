# Mindmap in Markdown

Here is a mindmap rendered as a fenced code block:

```mindmap
mindmap
  root((Web Development))
    HTML
      Semantic Tags
      Forms
      Accessibility
    CSS
      Flexbox
      Grid
      Animations
    JavaScript
      DOM
      Fetch API
      ES Modules
```

The mindmap above shows a basic web development topic tree.
